-- Script to list all records of the table second_table, ordered by score (top first)-- Lists all records of the table second_table.
-- Records are ordered by descending score.
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
USE `hbtn_0c_0`;

SELECT score, name
FROM `second_table`
ORDER BY score DESC;
