#!/usr/bin/python3
def pow(a, b):
    """
    Computes a to the power of b and returns the result.

    Args:
    - a: The base (integer or float).
    - b: The exponent (integer).

    Returns:
    - The value of a ^ b.
    """
    return a ** b
